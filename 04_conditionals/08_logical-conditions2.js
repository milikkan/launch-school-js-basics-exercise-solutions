// Predict the output of the following code:

if (true && false) {
  console.log('Yes!');
} else {
  console.log('No...');
}

// this code logs 'No...' to the console because the expression 'true && false' evaluates to false.