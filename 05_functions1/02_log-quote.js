brendanEichQuote();
// logs:
// Always bet on JavaScript.

function brendanEichQuote() {
  console.log('Always bet on JavaScript.');
}

// return value of the function is 'undefined' since we didn't explicitly return any value.